### Note
The menu-items branch is for my own testing related to restaurant menu items. It isn't relevant to the current assignment.

## 3/9 and 3/11 Assignment
1. The restaurants.txt file has been imported into the project directory so its text data can be accessed easily.
2. The program creates a Scanner, then enters a while loop to read through the data in the file and instantiate new Restaurant objects based on that data.
3. When the program creates a Restaurant, it is also added to an ArrayList so any or all of the Restaurants can be accessed from the same place later.
4. A random Restaurant from the ArrayList is chosen and its information gets printed to the console. This is helpful to test if the program is properly reading, storing, and displaying data from the file.
 5. The two major tasks in the program are in their own methods, meaning the code is cleanly divided into parts and it'll be easier to reuse them within other sections of the program if necessary.